// The palette for code that paints outside CSS: the history map's layer
// paint (makeHistoryData) reads it. styles/globals.css declares the same
// values as @theme tokens; keep the two in step.
export const palette = {
  surface: '#161616',
  accent: '#FFE520',
} as const;
